MFK｜Keeta Real External Acceptance｜Commander Handoff R1｜2026-09-23

CONTROL:
Pantonyeung/mfk #22

EXTERNAL ACCEPTANCE:
#162

CURRENT MAIN BEFORE THIS HANDOFF:
a0de5fed4327d02f0d893cee48170097a6a631db

NAVIGATION:
MFK 航海圖 V1.27｜Round028

CURRENT:
KEETA_K1_K8_SOURCE_TEST_DEPLOY_GREEN
REAL_EXTERNAL_ACCEPTANCE_ACTIVE

FIRST BREAK:
REAL_PROVIDER_EXTERNAL_ACCEPTANCE

==================================================
1. COMMANDER DECISION
==================================================

不再開新功能。
不因未完成真 Provider 驗收而預設要改 code。

Maximum active lanes = 2。

Lane A:
E1 Menu
→ BANK
→ E6 Sellability / Hours / REST / OPEN

Lane B:
E2 Real Test Order
→ E3 Confirm
→ E4 READY
→ E5 Lifecycle / Cancel
→ E7 After-sale（只在 test environment exposes 時）
→ E8 Commercial /order/get readback

Immediate NEXT:
START E1 + E2 ONLY.

==================================================
2. E1 MENU
==================================================

Admin：
1. preflight 完整 MFK canonical menu snapshot
2. verify counts / OpenItemCode completeness
3. submit exactly one controlled full-menu sync
4. capture taskId
5. capture real 1202 completion
6. capture 1201 picture completion if emitted
7. zero unexpected omissions
8. partial/provider errors must remain visible

No fake GREEN.

==================================================
3. E2 REAL ORDER
==================================================

Keeta test store：
1. place ONE real test order
2. capture real 1001
3. SMT receives once
4. ONE canonical MFK Order
5. ONE display number
6. providerRef linked
7. replay/duplicate creates no second Order / Display / Print
8. preserve commercial evidence

This same Order should continue into E3 / E4 / E8 where provider lifecycle allows.

==================================================
4. RED HANDLING
==================================================

If a real provider step fails：

1. freeze exact failing seam
2. preserve request / response / webhook / canonical evidence
3. classify:
   PROVIDER
   CONFIG
   RUNTIME
   MAPPING
   UNKNOWN
4. UNKNOWN external side effect = readback first, no blind retry
5. only open bounded repair after evidence proves an implementation defect

==================================================
5. EVIDENCE BUNDLE
==================================================

Every E-step records：

- acceptance_case_id
- timestamp
- source/main identity
- provider task/message/order IDs
- canonical order ID
- display number
- exact input/action
- provider response / webhook evidence
- authoritative readback
- duplicate/idempotency result
- screenshot/log/evidence link
- verdict

Allowed verdict:
GREEN
RED
EXTERNAL_TEST_ENV_NOT_EXPOSED

==================================================
6. FINAL GATE
==================================================

Only declare:

KEETA_FULL_PROVIDER_INTEGRATION_GREEN

after all Keeta test-environment-exposed E1–E8 seams have real provider evidence banked.

GitHub #162 Commander takeover comment:
5787184091


==================================================
7. EXECUTION STARTED
==================================================

GitHub #162:
comment 5787238095

Lane A:
KEETA-E1-MENU-20260923-01
ACTIVE

Exact operator path:
Admin
→ 渠道
→ 供應同步
→ Keeta 菜單同步
→ 預檢完整菜單
→ if complete: 同步完整菜單到 Keeta ONCE
→ 更新同步狀態
→ taskId / 1202 / optional 1201

Lane B:
KEETA-E2-ORDER-20260923-01
ACTIVE

Exact operator action:
Keeta test store
→ place ONE real test order
→ SMT first receipt
→ ONE canonical Order
→ ONE display number
→ preserve same Order for E3/E4/E8

STATUS:
E1_ACTIVE
E2_ACTIVE
WAITING_REAL_PROVIDER_EVIDENCE


==================================================
8. WORKER REPLAN｜DIRECT INTEGRATION FAST LOOP
==================================================

Control correction:
DIRECT_INTEGRATION_REAL_DEVICE_FAST_LOOP_LOCKED

GitHub #162:
comment 5787772300

Previous worker model:
E1 / E2 independent live acceptance lanes

SUPERSEDED AS WORKER EXECUTION MODEL.

New arrangement:

Worker A:
KEETA_ORDER_DIRECT_INTEGRATION_REAL_DEVICE_FAST_LOOP

- owns current live seam mutation
- historical V2 evidence reused as regression oracle
- minimum gate only: exact diff / focused tests / build / authority guard
- direct runtime/device
- GREEN bank
- RED exact FIRST_BREAK → smallest fix → redeploy → same-step retest
- no full matrix prerequisite

First target:
1001
→ dedupe / mapping
→ SMT reconcile
→ ONE canonical Order
→ ONE display
→ SMT/SMM visibility
→ merchant confirm

Worker B:
HISTORICAL_V2_EVIDENCE_REUSE_NEXT_SEAM_PREP

READ_ONLY default.

Only prepares:
- current Worker A seam
- ONE next seam

Return:
- exact historical evidence
- current MFK equivalent seam
- DO_NOT_REBUILD / DO_NOT_RETEST foundations
- minimum regression gate
- expected real-device behavior
- likely FIRST_BREAK
- smallest repair allowlist

Initial next seam:
merchant ACCEPT / provider confirm
→ same canonical Order visibility

Collision:
ONLY Worker A mutates current live seam.
Worker B cannot parallel-mutate.

Current main observed during replanning:
afb02b6e09f4f4458c8e556b85d9a7eb6e6ea4b0

STATUS:
WORKER_REPLAN_READY
DISPATCH_PAUSED
