MFK Keeta Direct Integration｜Worker A｜R1
Date: 2026-09-23
Role: CURRENT LIVE SEAM DRIVER / REAL DEVICE FAST LOOP
Mode: DIRECT_INTEGRATION_FIRST / HISTORICAL_EVIDENCE_REUSE / FIRST_BREAK_ONLY

CURRENT
- current MFK main fresh-read: dfe2b12922a8af7f8477232e28b5d93544c7fde7
- K1 runtime-affecting source remains afb02b6e09f4f4458c8e556b85d9a7eb6e6ea4b0
- main is only 2 documentation commits ahead of afb02b6; compare shows no runtime/product files changed.
- current seam: Keeta 1001 -> cloud PENDING_SMT -> SMT reconcile -> mapping -> ONE canonical Order -> ONE display -> ACK -> merchant decision.
- v2local/src/runtime/keeta-order-intake.ts contains:
  - 401 operator-visible attention
  - Admin-config-triggered retry
  - online/focus/visible reconcile
  - visible-only 5s fallback reconcile
  - providerRef dedupe through existing localRuntime path
  - no second Order/Pricing/Sync engine

HISTORICAL EVIDENCE REUSED
- Morefun-v2 #881 comment 5753715763: Keeta already integrated; DO_NOT_REBUILD provider/order/mapping/store-kernel foundations.
- Morefun-v2 #881 comment 5753817858: durable receipt/dedupe/mapping and historical live forward-chain evidence including provider Order -> canonical Order -> merchant ACCEPT -> provider confirm -> SMT/SMM same canonical Order.
- Historical V2 is regression oracle only, not current execution authority.

LAST_GREEN
- real provider 1001 -> MFK webhook ACK: GREEN from existing #162 evidence.
- current K1 bounded repair source: GREEN.
- V2 Local POS Smoke: 35809338650 SUCCESS.
- Builder: 35809458982 SUCCESS.
- public Runtime OTA published.

FIRST_BREAK
- current affected runtime has not yet been re-tested on the shop SMT after the afb02b6 repair.
- existing second controlled order remains the diagnostic subject.
- real-device certainty is UNKNOWN until the exact runtime is installed/confirmed and the same order is read back.
- no new code fix is justified before that real-device readback.

EXACT CHANGE THIS TURN
- product/runtime code: NONE.
- handoff branch/document only.
- DO NOT reapply 190d2b82 / 45bc3c95 / afb02b6; already landed on current main lineage.

MINIMUM TEST / AUTHORITY GATE
- afb02b6 -> current main compare: only 2 handoff docs changed.
- therefore tested runtime source identity is unchanged.
- reuse focused current-MFK proof 35809338650 + Builder 35809458982.
- authority invariant remains SMT local Formal Order authority; cloud/Admin is transport/readback/control plane only.

DEPLOY / RUNTIME
- release: runtime-candidate-mfk-afb02b6e09f4
- bundle: MoreFunOS-SMT-runtime-candidate-mfk-afb02b6e09f4.mfos
- SHA-256: bc64758000a879ff7d581b6fbd2fa7e2e045ff436e760fed3169dbeb45dddd14

REAL DEVICE RESULT
- NOT YET OBSERVED in this Worker session.
- this session has no direct physical SMT control surface; do not fake GREEN.

NEXT EXACT ACTION
1. On shop SMT: Recovery -> 檢查 Runtime OTA.
2. Install/confirm runtime-candidate-mfk-afb02b6e09f4.
3. Reuse existing second order KEETA-E2-ORDER-20260923-02. DO NOT create a third order.
4. Wait for automatic reconciliation; fallback loop is 5s while visible/online.
5. If still absent, press 手動接 Keeta 新單 exactly once.
6. Read back:
   - ONE provider identity
   - ONE canonical Order
   - ONE display number
   - ZERO duplicate Order / Display / Print Admission
   - intake attention/error code if RED
7. If GREEN: immediately continue merchant ACCEPT -> provider confirm on the SAME canonical Order.
8. If RED: report LAST_GREEN/FIRST_BREAK/EXPECTED/ACTUAL/SOURCE SHA/RUNTIME RELEASE/DEVICE RESULT and patch only that boundary.

STATUS
WAITING_REAL_DEVICE_SAME_ACTION_READBACK


REAL DEVICE MILESTONE｜MAPPING FIRST BREAK
Evidence: Owner SMT photo after manual Keeta receive.

CURRENT
K1_REAL_DEVICE_MAPPING_RED

LAST_GREEN
- SMT manual receive/reconcile path executed far enough to inspect the existing pending Keeta intent.
- Transport/auth is not the current first break.
- Mapping failure is now operator-visible as designed.

FIRST_BREAK
KEETA_ORDER_MAPPING_REQUIRED:MF-ORDER-SKU-NG-01

EXPECTED
Provider SKU/OpenItemCode resolves through current Admin channelMapping or unique canonical productCode/id, then the existing SMT localRuntime creates ONE canonical Order + ONE display and ACKs the same provider identity.

ACTUAL
SMT shows repeated mapping attention for MF-ORDER-SKU-NG-01 and creates no local Order. This is fail-closed behavior, not a silent intake failure.

SOURCE
- current main observed: f1aa12d1efd204591bf641783bd305c07e185e3d
- v2local/src/runtime/keeta-order-intake.ts blob: 300605dc2206dee66d38fad920ae562746b46db3
- runtime logic unchanged from bounded K1 repair line.

FAILURE LAYER
CONFIG / PROVIDER_TO_CANONICAL_PRODUCT_MAPPING

SMALLEST SAFE FIX
Add ONE explicit Admin mapping:
providerItemId = MF-ORDER-SKU-NG-01
productId = <correct current canonical MFK product ID>
status = MAPPED
Then save/publish an Admin config revision and let SMT receive the new LKG. The existing PENDING_SMT order should be reconciled again by the same K1 path. No third Keeta order and no intake-engine code change.

BLOCKER
The correct canonical product target is not evidenced by current source/history. Exact-code searches found no grounded mapping for MF-ORDER-SKU-NG-01. Historical proven item MF-SIT-SKU-01 / SIT 測試餐 A 更新 is a different provider identity and must not be reused by guess.

NEXT
Owner must identify which MFK product was selected in this Keeta test order (local product name is sufficient). Then create/publish the single explicit mapping and retest the SAME pending order.

STATUS
FIRST_BREAK_FOUND -> SMALLEST_FIX = ONE_EXPLICIT_MAPPING


OWNER CORRECTION｜K3 MENU LIVE SYNC FIRST

The previous mapping-first interpretation is superseded for execution order.

Reason:
MF-ORDER-SKU-NG-01 is a provider-side test product that was not created from the current MFK full-menu sync. One-off mapping it now would only make the old test artifact pass and would not prove the intended current catalog path.

CURRENT
K3_MENU_LIVE_SYNC_FIRST

LAST_GREEN
- Current K3 menu projection/source/test/deploy exists.
- Inactive hidden donor products are excluded from provider payload.
- Active products remain fail-closed on invalid category/price.
- Provider token error 115000200 recovery path is deployed.
- Current K3 output uses SKU openItemCode = productCode and SPU openItemCode = SPU:<productCode>.
- Current K1 resolver directly matches provider OpenItemCode to canonical productCode/id.

FIRST_BREAK
LIVE_FULL_MENU_SYNC_NOT_YET_EXECUTED / REAL_1202_NOT_YET_READ_BACK

CORRECT CHAIN
MFK Admin canonical catalog
→ full menu preview
→ token validation/recovery
→ /product/menu/sync
→ taskId
→ 1202 completion
→ optional 1201 picture completion
→ create a Keeta test order from the newly synced MFK menu
→ K1 direct productCode/OpenItemCode resolution
→ ONE canonical Order
→ ONE Display
→ merchant ACCEPT/provider confirm

NO LONGER NEXT
Do not patch MF-ORDER-SKU-NG-01 to an arbitrary MFK product.
The existing legacy test order is evidence of correct fail-closed mapping only.

LIVE EXECUTION NEXT
1. Admin → Keeta 菜單同步 → 預檢完整菜單.
2. Require non-empty clean active catalog snapshot and no validation errors.
3. Submit full menu sync once.
4. Capture taskId.
5. Wait for real 1202; require zero provider errors.
6. Capture 1201 if emitted.
7. Only then create the next test order from the synced MFK menu.
8. Re-run K1.

CURRENT MAIN
71d1fe505b905466475567647bf6e9bca442e972

GITHUB #162
5788093815

STATUS
K3_MENU_LIVE_SYNC_FIRST / K1_ORDER_RETEST_AFTER_MENU_GREEN
