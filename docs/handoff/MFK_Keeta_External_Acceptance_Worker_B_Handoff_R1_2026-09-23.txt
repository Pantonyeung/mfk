MFK Keeta Real External Acceptance｜Worker B｜R1
Date: 2026-09-23
Role: E2 → E3 → E4 → E5 → E7(if exposed) → E8
Mode: REAL_EXTERNAL_ACCEPTANCE / NO NEW FEATURE / NO CODE MUTATION UNTIL REAL RED PROVES IMPLEMENTATION DEFECT

FRESH READ
- current main: bae339342bc000a8fb69753be91ad4d06a634096
- #22 latest controlling: 5786932900 remains Keeta K1–K8 source/test/deploy green; 5787214530 is unrelated SMM UI review
- #162 commander: 5787238095
- navigation: docs/navigation/MFK_航海圖_V1.27_Round028_2026-09-23.txt
- runtime product bank: a6cca587b347cda90cab94e5ff4a647aacacfd24
- K1: contracts/keeta-order-intake-v1.ts; v2local/src/runtime/keeta-order-intake.ts; v2admin/keeta-runtime.ts
- K2/K5: v2local/src/runtime/keeta-provider-commands.ts; v2local/src/runtime/local-runtime.ts; v2admin/keeta-runtime.ts
- K6: v2local/src/runtime/keeta-order-lifecycle.ts; v2admin/keeta-runtime.ts
- K7: v2local/src/runtime/keeta-after-sale.ts; v2admin/keeta-runtime.ts
- K8: v2admin/keeta-runtime.ts; v2admin/src/keeta-live-client.ts

SOURCE REALITY CONFIRMED
- K1 provider identity dedup: providerRef = KEETA:<providerOrderId>; local createOrder returns existing order when providerRef already exists.
- K1 provider message dedup: webhook dedup key = webhook:dedup:<messageId>; repeated identical provider message ACKs without second intent.
- K1 canonical ACK persists canonicalOrderId + canonicalDisplay and rejects conflict.
- K2/K5 provider command record is keyed by providerOrderId + action; SUCCESS replay returns IDEMPOTENT; UNKNOWN blocks blind resend pending readback.
- K5 /order/prepare is READY only.
- K6 lifecycle events 1002/1003/1004/1006/1008 link to the same committed providerOrderId/canonicalOrderId; repeat providerMessageId is idempotent; completed-vs-cancel remains explicit conflict.
- K7 1005/1007 links afterSaleOrderId to same canonical Order; final decision repeat is idempotent; conflicting final decision blocked; partial refund APPLY requires matching PREVIEW fingerprint.
- K8 /order/get refresh requires committed Order, exact providerOrderId match, and stores provider commercial facts separately from canonical transaction truth.

E2
ACCEPTANCE_CASE_ID: KEETA-E2-ORDER-20260923-01
CURRENT: E2_ACTIVE / ARMED
PROVIDER_ORDER_ID: PENDING
CANONICAL_ORDER_ID: PENDING
DISPLAY_NUMBER: PENDING
FIRST_BREAK: REAL_TEST_ORDER_NOT_YET_PLACED
VERDICT: NOT YET ELIGIBLE — no provider action has occurred yet

UNAVOIDABLE OPERATOR ACTION
Place exactly ONE real order in the Keeta test store.
Do not place a second order while result certainty is unknown.

AFTER ORDER IS PLACED
Read back real 1001 first, then prove:
- providerOrderId + messageId
- receivedAt
- SMT first-seen time
- mapping result
- ONE canonical Order
- ONE display number
- providerRef linked
- print admission count
- ZERO duplicate Order / Display / Print Admission
If any result is UNKNOWN: STOP and readback first. No blind retry.

NEXT
Wait for Owner/operator to place the one real Keeta test order, then continue E2 evidence capture before E3.


E2 OPERATOR INPUT RECEIVED
- Owner/operator reports exactly ONE real Keeta test order has been placed.
- This satisfies INPUT only. It does NOT prove 1001 receipt, SMT commit, canonical identity, display identity, print admission, or duplicate safety.
- Authoritative readback has not yet been obtained by Worker B.
- Current remote execution device "Panton" is OFFLINE, so Worker B cannot inspect authenticated SMT/Admin runtime directly.
- Public unauthenticated web access is not accepted as a substitute for authenticated canonical/provider readback.

CURRENT
E2_ORDER_PLACED_BY_OPERATOR / READBACK_REQUIRED

PROVIDER_ORDER_ID: UNKNOWN
MESSAGE_ID: UNKNOWN
CANONICAL_ORDER_ID: UNKNOWN
DISPLAY_NUMBER: UNKNOWN
PRINT_ADMISSION_COUNT: UNKNOWN
DUPLICATE_RESULT: UNKNOWN

VERDICT
NOT YET ELIGIBLE

FIRST_BREAK
E2_AUTHORITATIVE_RUNTIME_READBACK_NOT_AVAILABLE_TO_WORKER

SAFE NEXT
Obtain authenticated SMT/Admin runtime readback for the one existing order.
Do NOT place another test order.


E2 ACCESS CONSTRAINT UPDATE
- Owner is away from home and cannot bring the Remote Desktop / shop execution device online.
- This is an execution-surface availability constraint, not a provider RED and not an implementation defect.
- No second test order will be placed.
- No code mutation is authorized.
- E2 remains parked at authoritative readback.

CURRENT
E2_ORDER_PLACED_BY_OPERATOR / AUTHENTICATED_RUNTIME_READBACK_DEFERRED

CLASSIFICATION
EXTERNAL_ACCEPTANCE_EXECUTION_SURFACE_UNAVAILABLE

VERDICT
NOT YET ELIGIBLE

SAFE NEXT
Resume the same E2 case only when an authenticated SMT/Admin runtime readback surface is available. Preserve the single existing provider order identity. Do not create a replacement test order unless the original order is proven unusable by authoritative provider/canonical evidence.


OWNER DIRECTIVE UPDATE — OPERATIONAL SMT CONNECTION FIRST
Owner clarified priority: Keeta must be operationally connected into SMT now. Automatic receive remains the primary path. If automatic receive is unavailable or missed, SMT must offer a manual receive fallback. Owner does not want external acceptance bookkeeping to block operational usability.

CURRENT SOURCE REALITY
- Keeta 1001 live webhook → pending intent → SMT canonical intake path already exists.
- SMT auto receive is installed on app startup, focus, network-online and KEETA_ORDER_AVAILABLE doorbell.
- SMT merchant manual ACCEPT button already exists after the Keeta order is present locally.
- Missing operator surface was a manual pull fallback before local order admission.

IMPLEMENTED
Commit: 1b63fc6eb70ced899b93ba02f5fb9cf143975000
File: v2local/src/presentation/RuntimeOrdersWorkspace.tsx
Change:
- Added "手動接 Keeta 新單" control in SMT Order Manager.
- The control calls the SAME reconcileKeetaOrderIntake path used by automatic intake.
- No second Order Engine / no cloud Formal Order writer / no second pricing/payment/print authority.
- After pull, SMT refreshes local order list.
- Intake attention is surfaced when mapping/intake needs human handling.

VERIFICATION
GitHub Actions: V2 Local POS Smoke
Run: 35807329071
Job: 107010963021
Result: SUCCESS
- Unit tests GREEN
- Build GREEN
- Local authority + bounded cloud seam proof GREEN
- Static bundle proof GREEN

DEPLOYMENT REALITY
- Source/main is GREEN.
- Existing physical SMT device is offline, so this new UI fallback cannot be installed/read back on that device at this moment.
- The physical-device offline state does NOT remove the existing cloud 1001 webhook/pending intent.
- Formal Order must still be created by SMT; we will not create a cloud-side Formal Order just to bypass the offline device.

CURRENT
KEETA_AUTO_INGRESS_CONNECTED_IN_SOURCE
KEETA_MANUAL_PULL_FALLBACK_SOURCE_GREEN
PHYSICAL_SMT_UPDATE_PENDING_DEVICE_ONLINE


E2 ROOT-CAUSE CORRECTION AFTER OWNER PHOTO EVIDENCE
Owner photo evidence proves real Keeta event 1001 reached https://admin.morefunos.com/api/keeta/webhook at 2026-09-23 09:30:51 and MFK returned {"code":0,"data":{},"message":"Success"} in 512ms.
Observed provider envelope:
- eventId: 1001
- appId: 3419700273
- messageId: 2102571364841029696
- shopId: 721578302
- provider order identity visible in payload: orderViewId/orderViewIdStr 5313441990145415

CORRECTION
Remote Desktop / home PC is NOT the SMT runtime dependency. Previous diagnosis conflated an optional remote-control surface with the POS runtime.
Technical reality:
- GitHub = source authority.
- admin.morefunos.com / Cloudflare Worker = live cloud backend/webhook edge.
- SMT UI/runtime executes inside the POS Android Carrier/WebView (origin appassets.androidplatform.net) and consumes signed .mfos Runtime OTA.
Therefore provider ingress can be GREEN while the shop SMT still runs an older runtime that does not contain current Keeta K1 intake.

REAL FIRST BREAK
Provider → MFK webhook ingress: GREEN by real provider evidence.
MFK cloud 1001 handler/source: present.
SMT installed runtime: current public OTA before this correction was runtime-candidate-mfk-5e6e64ecd032, which predates K1/K2/K5/K6/K7/K8.
Therefore the operational break is CURRENT_KEETA_INTAKE_RUNTIME_NOT_YET_INSTALLED_ON_SMT, not Remote Desktop.

REPAIRS / DEPLOYMENTS COMPLETED
1. SMT manual fallback source:
   commit 1b63fc6eb70ced899b93ba02f5fb9cf143975000
   adds "手動接 Keeta 新單" using the SAME reconcileKeetaOrderIntake path.
   V2 Local POS Smoke 35807329071 SUCCESS.

2. Admin cloud intake readback:
   runtime endpoint /admin/orders/intake
   Admin UI shows PENDING_SMT vs COMMITTED, providerOrderId, messageId, canonicalOrderId, display.
   source through 40a503b08e294c833849b1c6e7ccd1813dd44927.
   deploy trigger 73445806271bb5f498173975e2c289d95fa5dc41.
   deploy-mfk-admin run 35807997961 SUCCESS.

3. SMT Runtime OTA published from exact MFK source 73445806271bb5f498173975e2c289d95fa5dc41:
   release runtime-candidate-mfk-73445806271b
   bundle MoreFunOS-SMT-runtime-candidate-mfk-73445806271b.mfos
   SHA-256 275f87a59a072419b079bb031fbfb3e8a1d1d84df72737bae16604d56a1569a6
   Builder run 35808028898 SUCCESS
   public readback MFK_RUNTIME_OTA_PUBLISHED

OPERATIONAL NEXT
At the shop SMT:
- Recovery → 檢查 Runtime OTA
- offered release must be runtime-candidate-mfk-73445806271b
- install/activate
- current runtime auto-runs Keeta pending-order reconciliation on boot/focus/online/doorbell
- if automatic pull is missed, use "手動接 Keeta 新單"
- the already-pushed provider order must be reused; DO NOT place another test order until readback of this order is known.

E2 CURRENT
PROVIDER_1001_DELIVERY = GREEN (real provider evidence)
WEBHOOK_ACK = GREEN (code 0 Success)
CANONICAL_ORDER_ADMISSION = PENDING SMT runtime update/readback
DUPLICATE_ORDER/DISPLAY/PRINT = PENDING readback


E2 SECOND REAL ORDER AUTHORIZED BY OWNER
Owner explicitly supersedes the earlier one-order-only stop for this case:
- first test order is excluded from acceptance judging;
- preserve its evidence but do not use it as the acceptance subject;
- second real Keeta order is now the controlled E2 acceptance subject;
- do not create a third order unless explicitly authorized after readback.

DOUBLE-CONFIRM BEFORE SECOND ORDER
1. Owner screenshots show current SMT Runtime UI contains "手動接 Keeta 新單", proving the post-fix runtime containing commit 1b63fc6eb70ced899b93ba02f5fb9cf143975000 is active on the shop SMT.
2. Current source startup installs both installSmtAdminAutoSync() and installKeetaOrderIntake().
3. SMT WebSocket connects to wss://admin.morefunos.com/api/admin-sync/events?storeId=MF01.
4. Successful Keeta 1001 webhook extracts baseOrder.orderViewId/orderViewIdStr and sends KEETA_ORDER_AVAILABLE doorbell.
5. SMT receives KEETA_ORDER_AVAILABLE and automatically calls reconcileKeetaOrderIntake().
6. Reconcile fetches /api/keeta/smt/orders/pending, maps products, creates ONE local canonical Order using providerRef dedup, and ACKs canonicalOrderId + canonicalDisplay.
7. Independently of doorbell, intake also reconciles at runtime startup, browser focus, and network-online.
8. Manual "手動接 Keeta 新單" invokes the SAME reconcile path only as fallback.

EXPECTED SECOND ORDER BEHAVIOR
Keeta submit → real 1001 → MFK webhook ACK → cloud PENDING_SMT → automatic SMT pull → canonical Order appears in 平台訂單.
If auto pull does not surface within a few seconds:
- press "手動接 Keeta 新單" exactly once;
- no third order;
- classify using cloud intake readback PENDING_SMT vs COMMITTED and any SMT intake attention.

NEW ACCEPTANCE CASE
KEETA-E2-ORDER-20260923-02
FIRST ORDER
EXCLUDED_BY_OWNER_FROM_ACCEPTANCE


K1 LIVE RED ROOT-CAUSE AUDIT AFTER SECOND ORDER
Owner reports second controlled order waited >1 minute with:
- no SMT platform order,
- no automatic operator prompt,
- Admin normal Orders surface showing no order.

AUTHORITATIVE FLOW RECONFIRMED
Keeta 1001
→ https://admin.morefunos.com/api/keeta/webhook
→ KeetaRuntimeStore durable order:intent:<providerOrderId> state PENDING_SMT
→ AdminSyncStore is transport/doorbell only (KEETA_ORDER_AVAILABLE)
→ SMT authenticated GET /api/keeta/smt/orders/pending
→ SMT maps against published Admin catalog/channelMapping
→ existing SMT localRuntime creates ONE canonical Order + Display
→ SMT ACKs canonicalOrderId + canonicalDisplay
→ cloud intent becomes COMMITTED
→ SMT manual ACCEPT then local ACCEPT first → Keeta /order/confirm
→ SMT READY local first → Keeta /order/prepare
→ 1002/1003/1004/1006/1008 lifecycle evidence links same canonical Order
→ 1005/1007 after-sale links same canonical Order
→ /order/get commercial readback stays reconciliation evidence only.

ADMIN ROLE
Admin is NOT an order relay or Formal Order authority.
Admin publishes catalog/mapping/channelPolicy/store config consumed by SMT.
The shared admin.morefunos.com Worker hosts the provider edge and doorbell transport, but Admin UI does not create the Order.
Normal Admin Orders/read model only receives SMT projection AFTER SMT canonical commit.
Therefore a PENDING_SMT provider order is not expected to appear in normal Admin Orders.
Dedicated diagnostic/readback is under Channels → 接單規則 → Keeta 新單入口／SMT 接收狀態.

WHY "ALL TESTS GREEN" DID NOT MEAN LIVE GREEN
K1–K8 source/test/deploy were banked GREEN:
- K1 #140: 35796407931 PASS
- K2/K5 #147: 35798128788 / 35798153187 PASS
- K6 #151: 35800516980 / 35800410086 PASS
- K7 #154: 35801776639 / 35801098970 PASS
- K8 #158: 35802522878 PASS
Those proved source contracts and deployed seams.
#162 exists specifically because real provider external acceptance was still pending.
Historical MoreFunOS also had prior live 1001→confirm→prepare capability evidence, but current MFK changed final Order authority to SMT local commit, so historical live proof cannot substitute for current MFK live acceptance.

SOURCE DEFECTS FOUND IN CURRENT K1 AFTER REAL RED
1. /smt/orders/pending HTTP 401 was silently returned with ZERO operator error.
2. Intake authorization/mapping depends on Admin LKG/ACK, but Keeta intake did not reconcile again when Admin sync completed; startup race could strand PENDING_SMT.
3. Doorbell was primary with no bounded periodic reconciliation; a missed doorbell could strand PENDING_SMT until focus/online/manual action.
4. Mapping failures were stored only in localStorage attention and were not automatically surfaced in SMT UI.
5. Current source has no true incoming-order modal/sound notification. Order visibility depends on successful local commit and queue count/list refresh.

BOUNDED REPAIR LANDED
- 190d2b82da64aadbc013ab0309d558eae91786da
  close silent 401 + retry after Admin sync + visible-only 5s fallback reconcile
- 45bc3c95049001aca4addfb103fc10a701419a77
  surface exact Keeta intake failure codes in SMT
- afb02b6e09f4f4458c8e556b85d9a7eb6e6ea4b0
  explicit navigator online guard correction

Verification:
- V2 Local POS Smoke 35809338650 SUCCESS
- unit tests / build / local authority guard / static bundle all GREEN
- added tests prove unauthorized transport and mapping RED are visible instead of disappearing.

NEW OTA
release: runtime-candidate-mfk-afb02b6e09f4
Builder run: 35809458982 SUCCESS
bundle: MoreFunOS-SMT-runtime-candidate-mfk-afb02b6e09f4.mfos
SHA-256: bc64758000a879ff7d581b6fbd2fa7e2e045ff436e760fed3169dbeb45dddd14
public readback: MFK_RUNTIME_OTA_PUBLISHED

NEXT
Do not create a third order yet.
Install runtime-candidate-mfk-afb02b6e09f4 on SMT.
The existing second order is the diagnostic subject:
- if cloud has it PENDING_SMT, startup/5s fallback should pull it automatically;
- if unauthorized, SMT now shows KEETA_SMT_UNAUTHORIZED;
- if mapping fails, SMT now shows exact KEETA_ORDER_MAPPING_* code;
- if no order and no intake attention, inspect Admin Channels → 接單規則 → Keeta 新單入口; empty there means second 1001 is not in current cloud intent store and the break is before SMT pull.


ADMIN + MENU LIVE RED REPAIR｜2026-09-23 10:26 HKT

OWNER EVIDENCE
Admin Platform Management showed:
- KEETA LIVE CONNECTION = CONNECTED
- Webhook Accepted = 25
- latest event currently 1005
- no Keeta order intake state visible on the Platform Management overview
- menu preflight produced repeated:
  KEETA_MENU_PRODUCT_CATEGORY_REQUIRED:<productId>
  KEETA_MENU_PRODUCT_PRICE_INVALID:<productId>

MENU ROOT CAUSE PROVEN
The failing product IDs include:
- 5cef7d6f-dfc0-521c-a5b2-de9cabe99f23
- a68b0e24-2ffb-53b6-b832-918d931b5ca7
- dbb5aec7-cdbb-50ac-99e9-92065f421ae4
- 7359e606-82fd-5299-9b84-6bc522c69a8e
These are canonical donor HIDDEN / inactive products in admin-menu-seed-mf01-v2.ts.
They intentionally carry:
- active:false
- categoryId:""
- basePrice:""
Admin draft validation already permits this for inactive donor rows.
Keeta menu projection incorrectly validated ALL products before later filtering to active products.
This was an implementation defect in K3 preflight, not bad Owner menu data.

MENU REPAIR
- commit 35b845dd2b87be4ed5bcc95b8bec1d45c1d23f0d
- Keeta preflight now validates provider-bound ACTIVE products only.
- inactive hidden donor products remain in MFK catalog history but are not projected to Keeta.
- active product missing category/price still fails closed.
Regression:
- commit ef797ba6aaa0ece6f000a229f337f4445edc74f9
- 188 active donor products project; hidden donor product 5cef... is excluded.
- active invalid row still RED.

ADMIN ORDER VISIBILITY REPAIR
Admin is not Formal Order authority and normal Admin Orders only sees SMT projection after canonical SMT commit.
However the provider intake diagnostic was hidden under Channels → 接單規則 only, while Owner was checking Platform Management overview.
- commit e3f0a146b1ce7b3c1e669de23b5432a0c0f79ec0
- Platform Management overview now also shows Keeta 新單入口／SMT 接收狀態:
  PENDING_SMT vs COMMITTED
  providerOrderId
  messageId
  canonicalOrderId
  display
This is readback only; no cloud Order writer is introduced.

DEPLOY
Trigger: 1e8e79260477a9789f91b3643cc34d5a1b8c5169
Run: 35810598869 SUCCESS
Tests: 15 files / 88 tests PASS
Build: GREEN
Worker Version: e0bf9465-1f62-43b2-8c68-778a41e3a5c5

SMT CURRENT REPAIR
Latest published SMT Runtime remains:
runtime-candidate-mfk-afb02b6e09f4
Builder: 35809458982 SUCCESS
This runtime exposes transport/mapping errors and retries intake after Admin ACK/config + every 5s while visible/online.

NEXT SAME-ORDER DIAGNOSTIC
Do NOT place another order yet.
1. Refresh Admin Platform Management. Intake card is now on that same page.
2. Read current second-order state:
   - PENDING_SMT => cloud received 1001; break is SMT auth/mapping/commit.
   - COMMITTED => SMT already created canonical order; break is SMT presentation/readback.
   - no row => second 1001 never entered current order:intent store; inspect provider webhook evidence/identity.
3. Menu: press 預檢完整菜單 again. The inactive hidden-product false RED is repaired. Do NOT submit provider full-menu sync until preview counts/errors are read back clean.


OWNER SCREENSHOT READBACK｜2026-09-23 10:36 HKT

REAL ORDER STATE
Admin live intake now proves:
- pending = 3
- committed = 0
- latest visible provider order example: 5313440114243101
- message 2102584550541508653
- received 2026-09-23 10:23:15 HKT
- canonicalOrderId = none
- canonicalDisplay = none

CLASSIFICATION
PROVIDER → MFK webhook/intake persistence = GREEN.
The current FIRST_BREAK is definitively after cloud PENDING_SMT and before SMT canonical commit:
CLOUD_PENDING_TO_SMT_COMMIT_RED.

This is NOT an Admin relay problem. Admin is readback/control-plane only.

TOKEN / MENU REAL RED
Owner live screenshot:
KEETA_MENU_SYNC_PROVIDER_115000200:The access token does not exist, please check if the access token is correct.

Meaning:
- local encrypted token metadata claimed CONNECTED/expiry 2026-12-22
- provider itself rejects that accessToken as nonexistent
- webhook ingress remains independent and continues working
- provider outbound commands/menu sync are blocked until token is refreshed or reauthorized

TOKEN REPAIR
commit 1f1cd99e0fdd4f3483a6f9b15aedd9ce5c75823c:
- explicit provider token-invalid detection
- one safe refresh-token recovery attempt after explicit provider rejection 115000200
- exact request retried once only after provider rejected the first request (no unknown side effect)
- failed recovery marks REAUTH_REQUIRED
- token save clears invalid marker
- Admin token check now performs real provider read validation, not local expiry only

SMT PULL DIAGNOSTIC
same commit:
- /smt/orders/pending now records last authorized SMT pull:
  deviceId / observedAt / pendingCount
- Admin intake readback returns lastSmtPull

UI
c6f0707448c809a6ce3f49f2237a947f9d6578f7 + 1940e2ccb826bb98fb1457225e9f6687b4e982bb:
- Provider Token Validation surfaced
- SMT 最近拉單 timestamp + device ID surfaced in Platform Management

TEST
a90b94cf1ec5c71d41a0c0220613db475bb6ad1c
- explicit 115000200 → refresh token → menu sync retry success
- SMT pull diagnostic readback
Deploy trigger:
784c0d5d80b95d49e4bea203fdafffb6bfbaf1eb
Run:
35811354847 SUCCESS
15 test files / 89 tests PASS
Admin Worker:
5840ed55-068c-4406-b617-2aa8b5f80823

NEXT
1. Refresh Admin Platform Management.
2. Read "SMT 最近拉單":
   - 未見 = current SMT runtime is not reaching authorized pending endpoint (runtime/transport/version).
   - recent timestamp = SMT has pulled PENDING orders; remaining break is local mapping/commit and current afb runtime should surface exact intake error.
3. Press "檢查 Token":
   - TOKEN_USABLE_PROVIDER_CONFIRMED = token recovered/valid.
   - REAUTH_REQUIRED / unavailable = perform Keeta reauthorization.
4. Re-run menu PREVIEW only; if clean then submit one menu sync.
5. Do not create additional test orders until current 3 pending are classified/cleared.
