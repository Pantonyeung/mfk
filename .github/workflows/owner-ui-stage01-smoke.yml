name: owner-ui-stage01-smoke

on:
  push:
    branches:
      - work/MFK/OWNER-UI-STAGE01-TODAY-R1
  workflow_dispatch:

jobs:
  stage01:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: v2owner
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install
        run: npm install --no-audit --no-fund --legacy-peer-deps --package-lock=false
      - name: Test
        run: npm test
      - name: Build
        run: npm run build
